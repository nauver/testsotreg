/* Reveal, stagger and reading-progress effects */
(()=>{const bar=document.createElement("div");bar.className="progress";bar.setAttribute("aria-hidden","true");document.body.prepend(bar);const targets=[...document.querySelectorAll(".panel,.stat,.section-head,.card,.numbers,.number-item,.cta")];targets.forEach((el,i)=>{el.classList.add("reveal");el.style.transitionDelay=`${Math.min(i%6,5)*65}ms`});const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add("is-visible");io.unobserve(e.target)}}),{threshold:.13,rootMargin:"0px 0px -7% 0px"});targets.forEach(el=>io.observe(el));const update=()=>{const max=document.documentElement.scrollHeight-innerHeight;bar.style.width=`${max>0?scrollY/max*100:0}%`};addEventListener("scroll",update,{passive:true});addEventListener("resize",update);update()})();
